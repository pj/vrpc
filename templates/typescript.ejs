<% imports.forEach(function (imp) { %>
  import * as <%= imp %> from './<%= imp %>';
<% }) %>

<% versions.forEach(function (version, index) { %>
  <% if (version.state === 'active' || version.state === 'deprecated') { %>
    <%- include('typescript_class', {className: 'H_' + version.hash, version: version}); %>

    export type V_<%= index %> = H_<%= version.hash %>;
  <% } %>
<% }) %>

<% if (latest !== null) { %>
  <%- include('typescript_class', {className: 'Latest', version: latest}); %>
<% } %>
